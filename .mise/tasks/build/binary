#!/usr/bin/env bash
#MISE description="Build the binary"
#MISE depends=["build:wheel"]
uv sync --group build
echo -e "from maws.main import app\n\napp()" >/tmp/maws.py
uv run nuitka --onefile --remove-output -o dist/maws /tmp/maws.py
